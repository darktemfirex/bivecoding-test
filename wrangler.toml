name = "bivecoding-test"
main = "worker.js"
compatibility_date = "2024-05-24"

[site]
bucket = "."

[vars]
# OPENAI_API_KEY should be set as a secret using `wrangler secret put OPENAI_API_KEY`
# or in a .dev.vars file for local development.
